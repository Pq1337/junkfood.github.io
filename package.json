{
  "name": "jf1337-personal-site",
  "version": "1.0.0",
  "private": true,
  "description": "Personal website with a server-side DeepSeek proxy",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
